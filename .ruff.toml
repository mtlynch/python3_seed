src = ["app"]

exclude = [
    ".git",
    "third_party",
    "venv",
]

# Assume Python 3.13
target-version = "py313"

[lint]
select = [
  "I", # Enable isort rules.
]
ignore = []

[lint.isort]
force-single-line = true
